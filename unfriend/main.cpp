#include <iostream>

using namespace std;

int num, store[10000], temp;

int main()
{
    cin >> num;
    fill(store, store+10000, 1);
    for(int i = 0; i < num; i++){
        cin >> temp;
        store[temp] += store[i;

    }

    return 0;
}
